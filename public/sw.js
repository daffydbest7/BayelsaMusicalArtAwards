// Empty Service Worker to fulfill browser/PWA queries cleanly without 404 terminal logs.
self.addEventListener("install", () => {
  self.skipWaiting();
});

self.addEventListener("activate", (event) => {
  event.waitUntil(self.clients.claim());
});
